{
  "name": "chup-chuan",
  "version": "1.0.0",
  "private": true,
  "description": "Chụp Chuẩn – camera guide: point, follow the picture, green means shoot.",
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "ios:add": "npx cap add ios",
    "ios:sync": "npm run build && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
